import React from 'react' //impt + tabkey

function Footer({text}) {
  
  return (
    <header>
        <div className='container'>
            <h2> Feedback Footer UI</h2>
        </div>
    </header>
  )
}
export default Footer
